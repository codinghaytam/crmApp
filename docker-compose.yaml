version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agent_backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://ep-restless-surf-a9j3buon-pooler.gwc.azure.neon.tech/neondb?user=neondb_owner&password=npg_CVg2wv1lNtcT&sslmode=require&channelBinding=require
    ports:
      - "8080:8080"
    networks:
      - agentnet

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: agent_frontend
    ports:
      - "4200:4000"
    networks:
      - agentnet
    depends_on:
      - backend

networks:
  agentnet:
    driver: bridge

volumes:
  db_data:
    driver: local
